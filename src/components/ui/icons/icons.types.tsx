export type CustomIconProps =
  | {
      width?: number;
      height?: number;
      color?: string;
      stroke?: string;
      xmlns?: string;
      strokeWidth?: string;
    }
  | undefined;

export type IconType = 'home' | 'explore';
