import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  infoRow: {
    paddingHorizontal: 16,
    paddingTop: 10,
  },
  infoRowLabel: {
    marginBottom: 6,
  },
  line: {
    height: 1,
    marginTop: 10,
  },
});
