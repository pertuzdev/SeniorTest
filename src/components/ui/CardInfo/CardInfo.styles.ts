import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  card: {
    paddingVertical: 12,
    paddingHorizontal: 16,
    flex: 1,
    borderRadius: 10,
  },
  cardLabel: {
    marginBottom: 6,
    fontSize: 15,
  },
});
