import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  searchBar: {
    height: 50,
    borderWidth: 1,
    borderRadius: 8,
    margin: 16,
    paddingLeft: 16,
  },
});
